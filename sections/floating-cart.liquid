{% render 'floating-cart', color_scheme: section.settings.color_scheme %}

{% schema %}
{
  "name": "Floating Cart",
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1",
      "info": "t:sections.all.colors.has_cards_info"
    }
  ],
  "presets": [
    {
      "name": "Floating Cart"
    }
  ]
}
{% endschema %}
